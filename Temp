rtlCode = rtlCode.replace(
  /(\w+)\.simulate\('blur',\s*(\{[^}]*target:\s*\{[^}]*value:\s*'(.*?)'.*?\}\})\)/g,
  (match, fieldName, blurArgs, value) => {
    return `fireEvent.blur(${fieldName}, { target: { value: '${value}' } })`;
  }
);
